# Both domains must be outside the production by-judith.com domain.
# Use a random hex password so it is safe inside the database URL.
POSTGRES_PASSWORD=replace-with-random-hex
SHOP_DOMAIN=
API_DOMAIN=
NPM_NETWORK=npm_network
